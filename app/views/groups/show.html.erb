<h1> Group Show Page </h1>

<% @group.memberships.each do |membership| %>
  <% member = User.find_by(:id => membership.user_id) %>
  <ul>
    <li><%= member.email %></li>
  </ul>
  <% end %>

  form to join this group